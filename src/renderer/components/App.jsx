import React from 'react';

const styles = {
  app: {
    overflow: 'hidden',
  },
};

const App = () => (
  <div style={styles.app}>
    <h1>Hello, World with hot reloading?!</h1>
  </div>
);

export default App;
